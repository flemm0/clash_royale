load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code
  - grpc_server:
      host: clash_royale_etl_project
      port: 4000
      location_name: "clash_royale_etl_project"